# NEVER commit the signing private key!
keys/signing_key.json

# Python cache
__pycache__/
*.pyc

# Wrangler / Worker local state
node_modules/
.wrangler/

# Cron / runtime logs
logs/

# NOTE: public/snapshot.json IS committed -- it's the file the CDN serves.
# The cron job (update_and_push.ps1) regenerates and pushes it every minute.
